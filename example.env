DISCORD_BOT_TOKEN=YourToken
GPT_API=YourToken
Login_Notify_Channel_ID=Login_Notify_Channel_ID
Dashboard_Channel_ID=Dashboard_Channel_ID

==================================================================================================
第一步：開啟兩步驟驗證 (必要前提)
Google 規定必須先啟動兩步驟驗證，才能產生應用程式密碼。
前往 Google 帳號管理頁面。
點選左側選單的 「安全性」。
在「登入 Google 的方式」區塊中，確認 「兩步驟驗證」 已顯示為「已開啟」。若未開啟，請依照畫面指示完成設定。

第二步：產生應用程式密碼
在「安全性」頁面的搜尋框（或直接在頁面中尋找），搜尋 「應用程式密碼」 (App Passwords)。
註：若搜尋不到，可能是因為你沒有開啟兩步驟驗證，或是帳號受公司/學校管理員限制。
進入頁面後，輸入一個名稱（例如：Discord_Bot）來標記這組密碼。
點選 「建立」。
畫面上會出現一個 16 位元的黃色框框代碼（例如：abcd efgh ijkl mnop）。

EMAIL_USER=example@gmail.com
EMAIL_PASSWORD=程式密碼
==================================================================================================

DISCORD_NOTIFY_CHANNEL_ID=YourToken 接收機器人訊息的頻道ID(EMAIL用) 之後可能看複雜度再看要不要多個